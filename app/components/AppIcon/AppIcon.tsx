export const AppIcon = ({
  className,
}: {
  className?: string;
}) => {
  return (
    <img
      src="/chefnotes-logo.png"
      alt="Chefnotes logo"
      className={className}
    />
  );
};
